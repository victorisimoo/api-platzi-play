services:
  postgres:
    image: 'postgres:latest'
    restart: unless-stopped
    environment:
      - 'POSTGRES_DB=platzi_play_db'
      - 'POSTGRES_PASSWORD=victorisimo.platzi'
      - 'POSTGRES_USER=victorisimoo'
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/data/postgres
volumes:
    pgdata: